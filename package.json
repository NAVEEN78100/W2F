{
  "name": "finalwwf-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "main-app",
    "backend",
    "help-hub",
    "dashboard"
  ],
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:main": "npm --prefix main-app run dev",
    "dev:help": "npm --prefix help-hub run dev",
    "dev:dashboard": "npm --prefix dashboard run dev",
    "dev:all": "concurrently -n API,WEB,HELP,DASH -c green,cyan,magenta,yellow \"npm run dev:backend\" \"npm run dev:main\" \"npm run dev:help\" \"npm run dev:dashboard\"",
    "setup:all": "npm --prefix backend install && npm --prefix main-app install && npm --prefix help-hub install && npm --prefix dashboard install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
